/**
 * AT·OM — Hooks (Consumer API)
 * Le même flux circule à travers TOUTES les facettes du diamant
 *
 *   useGovernance()        → Propositions, votes, consensus
 *   useEconomy()           → Transactions UR, FlowKeeper
 *   useMapping()           → Couches historiques, patterns
 *   useIdentity()          → DID, souveraineté, credentials
 *   useInformationFilter() → Tri info, intention, lois universelles
 *   useResonance()         → Score, rang, forge, diamant
 *   useThreatMonitor()     → Alertes, menaces, sentinelle amygdalienne
 *   useDigestive()         → Digestion web, Arbre des Connaissances
 *   useTabSystem()         → Onglets dynamiques du panneau central
 */

export { useGovernance } from './useGovernance';
export { useEconomy } from './useEconomy';
export { useMapping } from './useMapping';
export { useIdentity } from './useIdentity';
export { useInformationFilter } from './useInformationFilter';
export { useResonance } from './useResonance';
export { useThreatMonitor } from './useThreatMonitor';
export { useDigestive } from './useDigestive';
export { useTabSystem } from './useTabSystem';
